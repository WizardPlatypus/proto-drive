<UserControl x:Class="ProtoDrive.Desktop.Views.RegisterPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ProtoDrive.ViewModels.Auth;assembly=ProtoDrive.ViewModels"
             xmlns:local="clr-namespace:ProtoDrive.Desktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">

    <d:DesignData.DataContext>
        <vm:RegisterViewModel/>
    </d:DesignData.DataContext>

    <Grid>
        <StackPanel Width="300" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="ProtoDrive Registration" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            <Label Content="Username" />
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20"/>
            <Label Content="Password" />
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,0"/>
            <Label Content="Repeat" />
            <TextBox Text="{Binding Repeat, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20"/>
            <Button Content="Log In" 
                    Command="{Binding RegisterCommand}" 
                    Padding="10" 
                    Margin="0,0,0,10"/>
            <Button Content="Back" 
                    Command="{Binding NavigateToLoginCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
